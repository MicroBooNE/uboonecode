<?xml version='1.0'?>

<!-- Input to Geometry/gdml/make_gdml.pl; define the GDML fragments
     that will be zipped together to create a detector description. 
     -->

<config>

   <constantfiles>

      <!-- These files contain GDML <constant></constant>
           blocks. They are read in separately, so they can be
           interpreted into the remaining GDML. See make_gdml.pl for
           more information. 
	   -->
	   

   </constantfiles>

   <gdmlfiles>

      <!-- The GDML file fragments to be zipped together. -->

      <filename> micro-rotations.gdml </filename>
      <filename> materials.gdml </filename>
      <filename> LAr1-wireplane.gdml </filename>
      <filename> micro-vertplane.gdml </filename>
      <filename> micro-groundplate.gdml </filename>
      <filename> micro-cathode.gdml </filename>
      <filename> micro-fieldcage.gdml </filename>
      <filename> micro-tpc.gdml </filename>
      <filename> micro-pmtdef.gdml </filename>
      <filename> micro-enclosureExtras.gdml </filename>
      <filename> micro-cryostat.gdml </filename>
      <filename> micro-enclosure.gdml </filename>
      <filename> micro-world.gdml </filename>

   </gdmlfiles>

</config>
